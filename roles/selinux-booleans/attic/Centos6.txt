setsebool -P httpd_can_sendmail 1
setsebool -P logging_syslogd_can_sendmail 1
setsebool -P httpd_can_network_relay 1
setsebool -P allow_ftpd_full_access 1
setsebool -P httpd_verify_dns on
setsebool -P httpd_can_network_connect on
# for Nginx
setsebool -P httpd_setrlimit on	
# Disable CGI
setsebool -P httpd_enable_cgi off
setsebool -P httpd_tty_comm off

# RSYNC
semanage permissive -a rsync_t

semanage permissive -a zabbix_agent_t

semanage fcontext -a -t httpd_sys_content_t "/usr/local/www(/.*)?"
#semanage fcontext -a -t httpd_sys_rw_content_t "/usr/local/www(/.*)MotoTest/log(/.*)?"
semanage fcontext -a -t httpd_sys_rw_content_t "/usr/local/www(/.*)?/MotoTest/log(/.*)?"
semanage fcontext -a -t httpd_sys_rw_content_t "/usr/local/www(/.*)?/uploads(/.*)?"

restorecon -Rv /usr/local/www
