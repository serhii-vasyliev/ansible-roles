This role was used for cloud instances.

It was not used on bare-metal servers due to the lack of files in the `/etc/systemd/resolved.conf.d` directory.

The `DNS=` and `FallbackDNS=` entries in `/etc/systemd/resolved.conf` were configured manually.

This question needs more clarification.

